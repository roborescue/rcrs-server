package maps.gml;

/**
   Enum constants describing the coordinate system for a GMLMap.
*/
public enum CoordinateSystem {
    /** Coordinates are in latitude-longitude. */
    LATLON,
    /** Coordinates are in meters. */
    M,
    /** Coordinates are in millimeters. */
    MM;
}